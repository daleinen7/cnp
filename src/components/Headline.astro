---
export interface HeadlineProps {
  headline: string;
}
const { headline, headingLevel } = Astro.props;

const Heading = `${headingLevel}`;
---

<Heading>{headline}</Heading>

<style>
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-weight: 700;
    font-size: 1.875rem;
    margin: 4rem 2rem 2rem;
  }
</style>
