---
const { image, alt, imageWidth, imageHeight } = Astro.props;
---

<img src={image} alt={alt} loading="lazy" class="wrapper" />

<style>
  img {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: auto;
    max-width: 80rem;
    margin: 0 auto;
    padding: 1rem;

    @media (width > 600px) {
      padding: 2rem;
    }
  }
</style>
